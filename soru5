#include <stdio.h>
#include <stdlib.h>

int n;
int main()
{
    printf(" sayi giriniz: ");
    scanf("%d", & n);

    for (int i = 1; i <= n; i++)
    {
        if (i % 3 == 0) continue;
        if (i > 50) break;
        printf("%d\n", i);
    }
    return 0;
}
